# Hedera application.properties overrides for Falcon network deploy.
# Real TSS-BLS block signatures; BOTH streams feed mirror + block node (HIP-1056/1081).
blockStream.streamMode=BOTH
tss.forceMockSignatures=false
tss.hintsEnabled=true
tss.historyEnabled=true
